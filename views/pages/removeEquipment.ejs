<%- include('../partials/header'); %>
<%- include('../partials/navbar', { back: '/workStationWindow'}) %>

<div class="container h-100">
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-10 col-md-8 col-lg-6">
			<form action="/removeEquipment/?_method=DELETE&view=true" method="POST">
				<!-- <div class="form-group">
  					<label class="form-label">Serial No</label>
  					<input type="text" class="form-control input-sm" id="Serial_No" name="Serial_No" placeholder="Serial No">
				</div> -->
				<div class="mb-3 w-50">
					<label class="form-label">Equipment: </label>
					<select id="Serial_No" name="Serial_No" class="form-control drop-down">
					<% for(const e of equipment) { %>
					<option value=<%= e.Serial_No %>> <%= e.E_Name %> - <%= e.Serial_No %></option>
					<% } %>
					</select>
	</div>
				<div class="form-group">
    				<button type="submit" class="btn btn-primary btn-customized">Delete</button>
  				</div>
			</form>
		</div>
	</div>
</div>

<%- include('../partials/footer'); %>